{
  "name": "soc-product-board-check",
  "version": "1.0.0",
  "description": "This action posts a comment to a PR when the PR is closed without a  trello ticket linked.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.2.4",
    "@actions/github": "^2.2.0"
  }
}
